import SwiftUI
import UIKit

// MARK: - Shared POI appearance constants

let poiUIColors: [UIColor] = [
    .systemBlue, .systemOrange, .systemGreen,
    .systemRed,  .systemPurple, .systemTeal
]

let poiColors: [Color] = [.blue, .orange, .green, .red, .purple, .teal]

let poiIconOptions = [
    "mappin", "person.2.fill", "door.left.hand.open",
    "fork.knife", "toilet", "cart.fill",
    "info.circle", "cup.and.saucer.fill"
]
